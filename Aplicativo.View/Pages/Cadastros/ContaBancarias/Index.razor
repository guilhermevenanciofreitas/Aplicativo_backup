@attribute [Page("Cadastros/ContaBancaria")]
@attribute [LoginRequired(true)]

@inherits IndexPage

<ComponentLayout OnLoad="Component_Load">
    <ListItemViewLayout @ref="ListView" Type="ContaBancaria">

        <ViewPages>
            <ViewContaBancaria @ref="View" ListView="ListView" />
        </ViewPages>

        <BtnPesquisa>
            <ListViewBtnPesquisa @ref="ListView.ListViewBtnPesquisa" OnClick="ViewLayout_Pesquisar">
                <ListViewBtnFiltro>
                    <ViewFiltro />
                </ListViewBtnFiltro>
            </ListViewBtnPesquisa>
        </BtnPesquisa>

        <BtnNovo>
            <ListViewBtnNovo @ref="ListView.ListViewBtnNovo" ListItemViewLayout="ListView" OnClick="ViewLayout_ItemView" />
        </BtnNovo>

        <BtnExcluir>
            <ListViewBtnExcluir @ref="ListView.ListViewBtnExcluir" ListItemViewLayout="ListView" OnClick="ViewLayout_Excluir" />
        </BtnExcluir>

        <!--<View>-->
        @*<ViewUsuario @ref="View" ListItemViewLayout="ListItemViewLayout"></ViewUsuario>*@
        <!--</View>-->
        @*<ItemView>
                @{
                    var item = (Usuario)context;

                    <ListItemText Primary="@item.Login">
                        <SecondaryContent>
                            <Typography Component="span" Variant="@TypographyVariant.Body2" Color="@Color.TextPrimary" Style="display: inline;">
                                @item.Login
                            </Typography>
                            — I'll be in your neighborhood
                        </SecondaryContent>
                    </ListItemText>
                }
            </ItemView>*@

        <GridView>
            <ListViewGridView @ref="ListView.ListViewGridView" Type="ContaBancaria" OnItemView="ViewLayout_ItemView">
                <GridColumn Field="Descricao" HeaderText="Descrição" Width="300"></GridColumn>
            </ListViewGridView>
        </GridView>
    </ListItemViewLayout>
</ComponentLayout>

@*@page "/Cadastros/ContaBancaria"

    @inherits IndexPage<ContaBancaria>

    <ListItemViewLayout @ref="ListItemViewLayout"
                        TValue="ContaBancaria"
                        OnPageLoad="ViewLayout_PageLoad"
                        OnPesquisar="ViewLayout_Pesquisar"
                        OnItemView="ViewLayout_ItemView"
                        OnDelete="ViewLayout_Delete">
        <View>
            <ViewContaBancaria @ref="View" ListItemViewLayout="ListItemViewLayout"></ViewContaBancaria>
        </View>
        <ItemView>
            @{
                var item = (ContaBancaria)context;

                <ListItemText Primary="@item.Descricao">
                    <SecondaryContent>
                        <Typography Component="span" Variant="@TypographyVariant.Body2" Color="@Color.TextPrimary" Style="display: inline;">
                            @item.Descricao
                        </Typography>
                        — I'll be in your neighborhood
                    </SecondaryContent>
                </ListItemText>
            }
        </ItemView>
        <GridView>
            <GridColumn Field="Descricao" HeaderText="Descrição" Width="300"></GridColumn>
        </GridView>
    </ListItemViewLayout>*@