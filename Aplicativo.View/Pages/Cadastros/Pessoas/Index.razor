@inherits IndexPage

<ComponentLayout OnLoad="Component_Load">
    <ListItemViewLayout @ref="ListItemViewLayout" Title="@TitleList">

        <ViewPages>
            <ViewPessoa @ref="View" Tipo="@Tipo" Title="@TitleView"></ViewPessoa>
        </ViewPages>

        <BtnPesquisa>
            <ListViewBtnPesquisa OnPesquisar="ViewLayout_Pesquisar">
                <ListViewBtnFiltro>
                    <ViewFiltro />
                </ListViewBtnFiltro>
            </ListViewBtnPesquisa>
        </BtnPesquisa>

        <BtnNovo>
            <ListViewBtnNovo OnClick="ViewLayout_ItemView" />
        </BtnNovo>

        <BtnExcluir>
            <ListViewBtnExcluir OnClick="ViewLayout_Excluir" />
        </BtnExcluir>

        <!--<View>-->
        @*<ViewUsuario @ref="View" ListItemViewLayout="ListItemViewLayout"></ViewUsuario>*@
        <!--</View>-->
        @*<ItemView>
                @{
                    var item = (Usuario)context;

                    <ListItemText Primary="@item.Login">
                        <SecondaryContent>
                            <Typography Component="span" Variant="@TypographyVariant.Body2" Color="@Color.TextPrimary" Style="display: inline;">
                                @item.Login
                            </Typography>
                            — I'll be in your neighborhood
                        </SecondaryContent>
                    </ListItemText>
                }
            </ItemView>*@

        <GridView>
            <GridViewItem OnDoubleClick="ViewLayout_ItemView">
                <GridColumn Field="RazaoSocial" HeaderText="Razão Social" Width="300"></GridColumn>
            </GridViewItem>
        </GridView>
    </ListItemViewLayout>
</ComponentLayout>

@*<ListItemViewLayout @ref="ListItemViewLayout"
                        TValue="Pessoa"
                        OnPageLoad="ViewLayout_PageLoad"
                        OnPesquisar="ViewLayout_Pesquisar"
                        OnItemView="ViewLayout_ItemView"
                        OnDelete="ViewLayout_Delete">
        <View>
            <ViewPessoa @ref="View" ListItemViewLayout="ListItemViewLayout" Tipo="@Tipo" Title="@TitleView"></ViewPessoa>
        </View>
        <ItemView>
            @{
                var item = (Pessoa)context;

                <ListItemText Primary="@item.RazaoSocial">
                    <SecondaryContent>
                        <Typography Component="span" Variant="@TypographyVariant.Body2" Color="@Color.TextPrimary" Style="display: inline;">
                            @item.RazaoSocial
                        </Typography>
                        — I'll be in your neighborhood
                    </SecondaryContent>
                </ListItemText>
            }
        </ItemView>
        <GridView>
            <GridColumn Field="RazaoSocial" HeaderText="Razão Social" Width="300"></GridColumn>
        </GridView>
    </ListItemViewLayout>*@